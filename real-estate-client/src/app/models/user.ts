export interface User {
    id: number;
    username: string;
    email: string;
    firstName: string;
    lastName: string;
    // Add other properties as needed (e.g., password, avatar, etc.)
  }
  